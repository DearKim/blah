# architecture — 기술 결정과 코드 구조

스택 선택, 디렉터리 구조, 콘텐츠 관리 방식 등 **코드가 어떻게 짜여 있는지/짜여 있어야 하는지** 를 다루는 폴더입니다.

## 무엇이 들어가나

- 사용 중인 라이브러리·프레임워크와 그 선택 근거
- `src/` 의 디렉터리 구조 계획과 파일 배치 원칙
- 정적 콘텐츠를 어떻게 모듈화·타이핑하는지

배포·SEO·호스팅은 [../deployment/](../deployment/) 로 갑니다. 디자인 시스템(컬러·레이아웃) 은 [../design/](../design/) 입니다.

## 문서 목록

| # | 문서 | 요약 |
|---|---|---|
| 01 | [tech-stack-01.md](tech-stack-01.md) | React 19 + Vite + Tailwind 4 + React Router 선택 근거, 채택하지 않은 대안 |
| 02 | [directory-layout-02.md](directory-layout-02.md) | `src/` 트리, 라우트 정의, 결정 원칙 |
| 03 | [content-strategy-03.md](content-strategy-03.md) | `src/content/*.ts` 데이터 모듈로 콘텐츠 관리, 새 제품 추가 절차 |

## 새 문서를 추가할 때

다음과 같은 새 의사결정이 발생하면 이 폴더에 새 문서를 만듭니다.

- 새 의존성 도입 (라이브러리 평가·결정 근거)
- 새로운 패턴 도입 (예: i18n, 다크 모드, MDX 도입 시)
- `src/` 구조의 큰 변경
- 빌드 파이프라인의 변경

번호는 다음 미사용 번호(`04`, `05`, …) 를 씁니다. 위 표에도 추가합니다.
